//-NOTE 呈现书籍列表
//-NOTE 导入基本视图
extends layout

block content
    h1= title

    ul
    //-NOTE 使用循环导出信息
    each book in book_list
        li
            a(href=book.url) #{book.title}
            |   (#{book.author.name})

    else
        li There are no books.